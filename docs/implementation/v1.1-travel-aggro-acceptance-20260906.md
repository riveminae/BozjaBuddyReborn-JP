# Travel aggro correction acceptance

Date: 2026-09-06
Branch: `feat/bocchi-navigation`
Baseline: `3ac0b685b6cb7b583242931360c0ac947759719a`
Requirements: sections 6 and 18; detailed design 6.3; P7-03 / audit GAP-02.

The overall objective remains full v1.1 requirements acceptance followed by a
verified main merge. This packet fixes one confirmed violation; it does not
declare Initialize, transfer, CE, UI restructuring, or live acceptance complete.

## Frozen checks (before production changes)

1. Travel cannot enter `RunDefend` before arrival, even with a saved legacy
   `FightBack` value. The only call to `RunDefend(objective, attackers)` is guarded
   by `UnderAttack(attackers) && arrived`. The controller never reads `AggroResponse`.
2. Configuration UI cannot select a travel-aggro policy. It explains fixed escape
   behavior in Japanese. It never reads or writes `AggroResponse`.
3. Configuration migration normalizes every non-`KeepRunning` serialized value,
   including configurations already at the current schema version. The enum and
   field remain readable for compatibility.
4. Existing arrived-objective combat, `RunDefend`, `Commit`, travel survival, and
   mount guards remain unchanged. No transfer callback or memory write is added.
5. Existing static checks remain intact. New guards must fail on the old behavior;
   subsequent verification uses the same guards without changing their inputs.

Commands (Python 3.13 / .NET 10, from repository root):

```text
python -B tools/validate_v110_contract.py
python -B tools/audit_visible_japanese.py
dotnet build BozjaBuddyReborn.csproj -c Debug --no-restore --nologo
dotnet build BozjaBuddyReborn.csproj -c Release --no-restore --nologo
git diff --check
```

Objective source comparison must also confirm criterion 4 and that existing
validator statements were only augmented, not weakened. CI must pass packet replay
and idempotency on the resulting commit.

## Required live acceptance (not replaceable by static checks)

On both supported fields, start with a legacy FightBack configuration, acquire
aggro before arriving, and confirm travel continues without BBR intentionally
dismounting or attacking. At the objective, confirm existing skirmish combat still
starts. Record version, territory, observed states, and outcome. Until this is
observed, this packet supplies code/static evidence only, not main merge approval.

## Observed evidence

- Before production edits, the augmented static contract exited 1 at
  `travel escape policy is not configurable` on the baseline's runtime read.
- After production edits, the same static contract, Japanese UI audit, Debug build,
  Release build, and `git diff --check` each exited 0.
- `RunDefend`, `Commit`, and `UnderAttack` method bodies were compared with `git show
  HEAD:Automation/BozjaController.cs` at the baseline above: all three were identical
  (Python source comparison, exit 0). `HolsterDriver` and mount handling were not edited.
- An in-memory `unittest.mock.patch` of `Path.read_text` replayed the unchanged
  validator against five independently mutated inputs: restored legacy FightBack,
  missing arrival guard, duplicate defense call, restored UI preference, and
  ineffective migration assignment. All five produced `CONTRACT FAIL`; the proof
  process exited 0 and wrote no source files.
- Added validator SHA-256 after the red/green sequence:
  `16eccf3544a627907eea82a0d668db65e8314844903be1004ffe9467a52dcd18`.
- Both local builds reported NU1900 because NuGet vulnerability data was unavailable;
  compilation succeeded with zero errors. No vulnerability check was disabled.
- This was a direct source review, not an independent reviewer verdict. User-disabled
  orchestration was not invoked. Live checks remain unexecuted.
