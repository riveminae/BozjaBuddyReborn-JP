# dist/*.zip is deliberately committed - it is what the repo serves.
Thumbs.db
desktop.ini
.DS_Store
*.user
.vs/
.idea/

# Build output from plugins/ sources (dist/*.zip is the published artifact)
bin/
obj/
